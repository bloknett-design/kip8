# DEPLOY — Tasks 366–369 (перенос пакета из kip8test в kip8)

## Заявка пользователя

> Task 366: баннер недоставленных показаний + дубли строк
> hozraschet_archive. Task 367: «упростим ещё — баннер убрать вовсе,
> жёлто-оранжевые значения карточек с недоставленными, зелёный после
> доставки; недельные/месячные красные по календарной неделе/месяцу».
> Task 368: №3/№11/№9 — ввод за период двух дат (дефолт — прошедшая
> календарная неделя / прошлый месяц); красный по ЗАКРЫТЫМ неделям/
> месяцам. Task 369: «В разделе КИП ИОС, при свайпе кнопки "Проекты"
> (по такому же принципу как "Приборы" и "Клапана") открывается список
> проектов, сгруппированный по столбцу Статус проекта» + «Перенос в
> боевой kip8».

## Состав пакета (из kip8test, коммиты 7f4289d → 2cda164 → e17c93c → 5e6e9ec)

| Task | Что вошло | kip8test SW | kip8 SW |
|------|-----------|-------------|---------|
| 366 | server_busy повторяем; серверный дедуп архива (FlowmeterArchive.gs под Utils.withLock, ключ meterId+prev+curr+dateCurr+entryType, окно 50 строк); баннер (в 367 заменён цветом значений) | v595 | v443 |
| 367 | баннер недоставленных убран ВООБЩЕ — жёлто-оранжевый `#f5a623/#c96e00` цвет ЗНАЧЕНИЙ карточек (приоритет над красным), зелёный после доставки (перерендеры); красный недельных/месячных по календарной неделе пн–вс и месяцу (регэкспы «Еженед.»/«Ежемес.»/«раз в месяц») | v596 | v444 |
| 368 | показания №3/№11/№9 за ПЕРИОД двух дат (дефолт — закрытая неделя/прошлый месяц; правка — даты записи; инвертированные — hard-отказ); красный по ЗАКРЫТЫМ календарным неделям/месяцам (`_recordCoversPeriod`); подписи-диапазоны «за ДД.ММ–ДД.ММ.ГГГГ г.» на карточках/в детальной/хронологии; сервер НЕ менялся (updateReading пишет datePrev/dateCurr как есть) | v597 | v445 |
| 369 | свайп кнопки «Проекты» в КИП ИОС (в любую сторону, как «Приборы»/«Клапана») → страница «Проекты по статусу»: группы по столбцу «Статус проекта» (Новый→Выполнен→Остановлен→Отменен/Отменён→неизвестные по алфавиту→«(без статуса)»; внутри — «Дата утв.» старые→новые), поиск, тап по группе → project-group с годами; тап — прежнее «По отделениям» | v598 | v446 |

## Перенесено

- `index.html` — де-изоляция (prepare-kip8-transfer.py при 5e6e9ec),
  дифф с kip8 = 518 строк, проверен пофрагментно — только зоны 366–369.
- `scripts/FlowmeterArchive.gs` — серверный дедуп Task 366 (копия 1:1
  из kip8test; node --check валиден). Code.gs и остальные .gs НЕ
  переносились — маршруты функционально идентичны (проверено
  множествами case-маршрутов), расхождения только в комментариях.
- `sw.js`: CACHE_VERSION `kipia-v442` → `kipia-v446`.
- Тесты: 4 новых (366–369) + 6 адаптированных из kip8test
  (357/358/365/flow-period-input/flowmeter-validation/extract-functions)
  с маппингом версий kipia-test-v{593,594,595,596,598,599} →
  kipia-v{441,442,444,445,446,447}; базовый бамп остальных 55 тестов
  (guard v443→v447, ассерты v442→v446); run-all.js +4 require.
- Прогон: **2977 passed / 0 failed** (2838 + пакет).
- Браузер `scripts/task366-369-browser-check.py` — **31/31**
  (порт 8969; десктоп тёмная: цвета по закрытым периодам — №2 зелёный/
  №4 красный/№3 зелёный/№11 красный/№9 зелёный/№5 «Ежемес.» красный;
  подпись-диапазон; баннера нет; форма №3 «Период с/по» = закрытая
  неделя; реальный ввод №11 → payload с границами закрытой недели →
  зелёный; offline-ввод №4 → жёлто-оранжевые значения → после
  _flushOutbox снова зелёный; свайп «Проекты» влево/вправо →
  «Проекты по статусу» с порядком групп и счётчиками 2/3/1/1/1/1,
  группа → годы, тап → «По отделениям»; мобайл 375; 0 JS-ошибок).
  VLM-пруфы: `scripts/task366-369-proof-{flow-projects,mobile}.png`.

## Серверные действия

**FlowmeterArchive.gs — обновлён пользователем в Apps Script вручную**
(подтверждено в чате): «Новая версия» с дедупом Task 366 уже развёрнута
раньше этого переноса. Ничего дополнительно делать НЕ нужно. Остальные
серверные скрипты не менялись.

## Откат

```bash
git revert <hash-коммита>          # клиент + тесты + SW v446→v442
# серверная часть (FlowmeterArchive.gs Apps Script) — уже в проде у
# пользователя; откатывать её вместе с клиентом не обязательно:
# клиент корректно работает и с дедупящим сервером (дедуп идемпотентен).
```

## Примечание

kip8test продолжает разработку с SW kipia-test-v598 (Task 369).
Следующий номер задачи: 370.
