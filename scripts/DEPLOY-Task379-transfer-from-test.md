# DEPLOY — Task 379: перенос из kip8test в kip8 (боевой)

**Дата:** 17.09.2026
**Источник:** kip8test deedbde (Task 379)
**Назначение:** kip8 (боевой прод)

## Что перенесено

- **index.html** — те же 8 правок task379-patch.py (якоря совпали
  побайтово — зоны Task 378 переносились идентично):
  1. Шахматка: ПУСТЫЕ ячейки значений светлой темы — #FFFFFF
     (было #eef0f2): `.ws-cell`, «.»-ячейка (`ws-dot-code`),
     свотч «.» в попапе; тёмная тема НЕ тронута; итоги Task 378
     остаются (фон шахматки и итогов совпадает).
  2. Окна «Мероприятия»/«Нормы» бара: раскрытие — ОВЕРЛЕЙ вниз
     поверх бара (габарит бара НЕ меняется): отрицательный
     margin-bottom = 95 − scrollHeight в _barExpSync/_barExpToggle,
     синхронный transition height+margin-bottom, CSS .ws-bar-open —
     z-index 55 + тень «парения» (0.38 тёмная / 0.22 светлая).
- **sw.js** — kipia-v455 → **kipia-v456** (guard v457).
- **tests/** — test-task379.js из kip8test с маппингом версий
  (kipia-test-v608→kipia-v456, v609→v457); адаптации test-task312 /
  test-task319 / test-work-schedule (светлые пустые #eef0f2 →
  #FFFFFF); базовый бамп 68 тест-файлов (v455→v456 ×ассерты,
  v456→v457 ×guards); run-all.js +1 require.
- **scripts/** — task379-patch.py, task379-bump-sw.py,
  task379-browser-check.py (порт 8987, localStorage БЕЗ префикса).

## Верификация

- Тесты: **3232/0** (3215 базовых + 17 новых).
- Браузер: **49/49** (порт 8987, kip8_session_token/app-theme без
  префикса; обе темы + мобайл 375; 0 JS-ошибок): computed фоны
  пустых/«.»-ячеек (#FFFFFF светлая / #eef0f2+brightness тёмная);
  геометрия .ws-bar-row 95px ДО/ВО ВРЕМЯ/ПОСЛЕ раскрытия, ряды
  «Итоги»/действий и сетка не уехали, низ окна ниже верха сетки
  (оверлей), z-index 55 + тень + отрицательная маржа; мобайл —
  окно норм и сетка не сдвинулись.
- VLM-пруф: белые пустые ячейки, тёмные линии, жёлтые коды, красные
  рамки выходных — живы.

## Серверных действий НЕ ТРЕБУЕТСЯ

Чистая клиентская правка (CSS/JS index.html). Apps Script не затронут.

## Живой прод

После пуша: GitHub Pages пересоберётся автоматически (CI).
Проверить: строка 105 sw.js на живом kip8 = `kipia-v456`;
маркеры «Task 379» в index.html живого прода.
