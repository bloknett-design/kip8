# DEPLOY — Task 392: перенос из kip8test (строка без скобок + раздел «СИЗ»)

**Дата:** 2026-09-22
**Репозиторий:** kip8 (боевой PWA)
**SW:** kipia-v467 → **kipia-v468**
**Перенос из:** kip8test @ 442d1a4 (Task 392)

---

## ЧТО ПЕРЕНЕСЕНО

1. **Строка «Общей» вкладки «Работники»** — ведущее число ТЕКУЩЕГО
   момента БЕЗ СКОБОК: «Работников на текущий момент **12** (2 мастера,
   5 дневных, 5 сменных).» (заявка: «12 без скобок»).
2. **Раздел «СИЗ»** — средства индивидуальной защиты, лист **«СИЗ»**
   файла **табель_КИП_ИОС** (образец — «Таблица СИЗ работникам КИП
   ИОС.xlsx», перечень — Приказ Минтруда России от 29.10.2021 N767н):
   - секция «СИЗ · средства индивидуальной защиты» в карточке каждого
     работника (страница «Работники»): записи с мета-строкой
     («выдано 17.08.2026 · срок 1 год · до 17.08.2027 · «примечание»»),
     кнопки **✎ (правка)** / **✕ (удаление)**, строка **«+ СИЗ…»**;
   - шторка «Новое СИЗ» / «Правка СИЗ»: работник (префилл), наименование
     (datalist из 8 стандартных позиций + свободный ввод), дата выдачи,
     срок годности (— / 6 мес. / 1 год / 1,5 года / 2 года / 3 года /
     До износа), примечание;
   - **дата окончания срока годности считается АВТОМАТИЧЕСКИ**
     (дата выдачи + срок годности, кламп к длине месяца:
     31.08 + 6 мес → 28.02; «До износа» — без даты) — по образцу файла;
   - удаление с подтверждением; зрители видят записи без кнопок;
   - до серверного деплоя — мягкая деградация («нет выданных СИЗ»,
     ошибки в тосте); локальный кэш хранит СИЗ (офлайн).

## СЕРВЕРНЫЕ ШАГИ — ВЫПОЛНИТЬ ВРУЧНУЮ В APPS SCRIPT
(ОДИН бэкенд Apps Script на kip8 и kip8test)

Полная инструкция — `scripts/DEPLOY-Task392-ppe-sheet-workers-count.md`
(копия из kip8test). Кратко:

1. **`Code.gs`** — в `doPost` после `case 'workSchedule.updateVacation':`
   добавить 4 case: `workSchedule.listPpe/addPpe/updatePpe/deletePpe`
   (копия из репозитория `scripts/Code.gs`).
2. **`WorkSchedule.gs`** — заменить целиком на версию из репозитория
   `scripts/WorkSchedule.gs` (добавлены `PPE_SHEET` + 4 метода СИЗ +
   хелперы `_ppeLookupEmployee/_ppeTermMonths/_ppeExpiry`).
3. **`PPEInit.gs`** — НОВЫЙ файл (копия из `scripts/PPEInit.gs`):
   вставить в проект → выбрать `ppeDeployAll` → ▶ Run (один раз) —
   создаст лист «СИЗ» с заголовками и добавит каждому АКТИВНОМУ
   работнику стандартный перечень из 8 позиций образца
   (костюм 1 год / ботинки 1,5 года / бельё нательное / куртка
   утеплённая 2 года «До износа» / каска защитная 2 года «До износа» /
   подшлемник / противогаз / очки закрытые «До износа»).
   Повторный запуск безопасен. Диагностика — `ppeStatus`.
4. **«Сохранить» → «Новое развёртывание»** (Веб-приложение, доступ
   «Любой», выполнение «от моего имени»).
5. Проверка: «Работники» → вкладка работника → секция «СИЗ» с 8
   записями → «+ СИЗ…» → «Добавить» → тост «СИЗ добавлено работнику».

Структура листа «СИЗ»: A id / B таб_номер (FK, текст) / C работник /
D должность (копии для читаемости листа — заполняются приложением) /
E наименование_СИЗ / F дата_выдачи / G срок_годности /
H дата_окончания (АВТО) / I примечание.

Статус серверных задач: **Task 384** (правка карточек) — ВЫПОЛНЕН
пользователем ранее; **Task 387** — опционален; **Task 392** —
требует шагов выше (до них раздел СИЗ пуст, кнопки вернут
«Unknown action: workSchedule.listPpe/…» в тосте).

---

## ПРОВЕРКИ ПЕРЕНОСА

- `scripts/task392-patch.py` — 18/18 якорей с первого прохода
  (index.html ×11, WorkSchedule.gs ×5, Code.gs ×1, PPEInit.gs создан).
- `scripts/task392-transfer.py`: sw.js kipia-v467→v468; тесты —
  guards v468→v469, ассерты v467→v468; `tests/test-task392.js` —
  копия из kip8test (v620→v468 / v621→v469); контентные адаптации
  (task391 ×5, task389 ×3, task390 ×5, task388 ×2, tab-numbers,
  task314, run-all.js).
- Тесты: `node tests/run-all.js` → **3603 passed / 0 failed**
  (3556 + 47 Task 392; kip8-специфичный test-task344 жив).
- Браузер: `python3 scripts/task392-browser-check.py` (порт 9007,
  localStorage без префикса) → **28/28** — те же результаты, что в
  kip8test: строка «…момент 6 (…)» без скобок вокруг 6; карточка с
  секцией СИЗ (записи/✎/✕/«+ СИЗ…»); шторка (префилл/datalist 8/
  опции 7/авто-дата); добавление/правка/удаление; мобайл 375;
  0 JS-ошибок ×2. Пробы: `task392-proof-*.png`.

## ОБНОВЛЕНИЕ САЙТА

GitHub Pages задеплоен автоматически (CI). Пользователям: обновить
страницу с очисткой кэша (Ctrl+Shift+R ×1–2) — Service Worker
поднимется до **kipia-v468**.
