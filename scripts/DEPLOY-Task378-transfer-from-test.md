# DEPLOY — Task 378: перенос из kip8test в kip8 (боевой)

**Дата:** 17.09.2026
**Источник:** kip8test 8a16d3e (Task 378)
**Назначение:** kip8 (боевой прод)

## Что перенесено

- **index.html** — те же 53 правки task378-patch.py + фикс sel-col
  (0.44/0.30): побайтово зоны Task 378 = kip8test (проверено диффом —
  удалены ТОЛЬКО старые значения: зебра Task 322, слабые линии,
  0.10/0.06/0.16/0.24-подсветки).
  1. Итоги учёта: ячейки значений #FFFFFF (светлая) / #eef0f2+
     brightness(0.88) (тёмная); нули → пустые; линии как в шахматке
     (значения/шапка/«Сотрудник»); зебра удалена; Часы/Переработка в
     тёмной — палитра светлой.
  2. Перекрестье: hover ФИО → строка; hover ячеек итогов → строка
     сетки+итогов (архив мимо); яркость = «сегодня» (0.30/0.22,
     шапка 0.40/0.26, sel-col 0.44/0.30).
  3. Окна «Мероприятия»/«Нормы»: полоса прокрутки скрыта (оба
     движка), tabindex=0 (клавиши ↑↓), значок .ws-bar-exp — клик
     раскрывает окно на весь текст (низ по количеству текста),
     повтор — 95px; контраст 0.45 (не активен) → 1 (hover/раскрыто).
- **sw.js** — kipia-v454 → **kipia-v455** (guard v456).
- **tests/** — 8 файлов из kip8test с маппингом версий
  (kipia-test-v607→kipia-v455, v608→v456): test-task378.js (+32) и
  адаптации 315/316/319/321/322/333/377; базовый бамп 67 файлов
  (v454→v455 ×ассерты, v455→v456 ×guards); run-all.js +1 require.
- **scripts/** — task378-patch.py, task378-browser-check.py.

## Верификация

- Тесты: **3215/0** (3183 базовых + 32 новых).
- Браузер: **70/70** (порт 8985, localStorage БЕЗ префикса —
  kip8_session_token/app-theme; обе темы + мобайл 375; 0 JS-ошибок).
- VLM-пруф: белые ячейки, тёмные линии, нулей в таблице итогов нет,
  скроллбара в окнах нет, шевроны на месте.

## Серверных действий НЕ ТРЕБУЕТСЯ

Чистая клиентская правка (CSS/JS index.html). Apps Script не тронут.

## Живой прод

После пуша: GitHub Pages пересоберётся автоматически (CI).
Проверить: строка 105 sw.js на живом kip8 = `kipia-v455`;
маркеры «Task 378» в index.html живого прода.
