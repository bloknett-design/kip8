# DEPLOY — Task 382 transfer from kip8test (kip8)

## Источник

kip8test commit `811d963` (Task 382, SW kipia-test-v611) → боевой kip8.

## Заявка пользователя

«В мобильной версии, на странице итогов учёта, в таблице вкладки месяц
изначальную ширину столбца с фамилиями сотрудников сделай по ширине
текста в ячейках как на вкладке год.»

## Перенесённые изменения (только клиент, серверной части НЕТ)

`scripts/task382-patch.py` — 6 правок index.html, побайтово = дифф
kip8test `e2eddb0 → 811d963`:

1. CSS-комментарий Task 334-блока — «ПОСЛЕ 42%-й доли» → «ПОСЛЕ базовой
   ширины» (доля стала фолбэком переменной);
2. базовое правило месяца `.ws-tt-table:not(.ws-tt-year) th/td.ws-tt-emp`
   — `width: 42%` → `width: var(--ws-tt-emp-w, 42%)` + комментарий
   Task 382 (по тексту, «как в году»; фолбэк — прежняя доля; эллипсис
   остался);
3. комментарий Task 336 — порядок late-правила `.ws-narrow`
   актуализирован;
4. `fonts.ready` — вызов `_measureTtEmpFullW` (после `_barExpSyncAll`);
5. НОВЫЙ метод `_measureTtEmpFullW` после `_measureEmpNarrowW`
   (max «№ таб. + 5px маржи + ФИО» строк и заголовка «Сотрудник» +
   паддинги 16 + 2 → `--ws-tt-emp-w` на body; при активном сужении —
   data-full-обмен + inline-№ таб. на время замера; невидимый контейнер
   пропускается; шторка⇄страница max; дедуп setProperty);
6. `_reapplyEmpNarrow` — вызов `_measureTtEmpFullW` (typeof-guard;
   после КАЖДОГО рендера итогов — месяц и год).

Контроль побайтовости: дифф `kip8/index.html` ↔ `kip8test/index.html`
после патча = 82 строки — РОВНО базовые репо-специфичные различия
(префиксы localStorage `kip8test:`/isolateLocalStorage, комментарии
тестового репо, URL Apps Script), как и ДО патча. Патч-скрипт
проверен на воспроизводимость: откат → запуск → diff с прямым
переносом — IDENTICAL.

## Service Worker

`scripts/task382-bump-sw.py`: `kipia-v458` → **`kipia-v459`**
(guard `kipia-v460`), 71 тест-файл (guards первыми, затем ассерты).
Повторный запуск падает на dup-защите без записи файла.

## Тесты

- `tests/test-task382.js` (+24) — скопирован из kip8test с маппингом
  `kipia-test-v611 → kipia-v459` / `kipia-test-v612 → kipia-v460`.
- Адаптации `test-task325.js` / `test-task327.js` / `test-task329.js`
  — скопированы из kip8test (зоны Task 382: `width: 42%` →
  `width: var(--ws-tt-emp-w, 42%)`; доля осталась фолбэком) с тем же
  маппингом версий; дифф с HEAD = только зоны адаптации + бамп версий.
- `tests/run-all.js` +1 require.
- **run-all: 3294 passed / 0 failed** (было 3270) — с первого прогона.

## Браузерная проверка (scripts/task382-browser-check.py, порт 8993)

localStorage БЕЗ префикса (`kip8_session_token` / `app-theme`).
**33/33** — результаты идентичны kip8test (порт 8992):

- мобайл 375, обе темы: M1 `--ws-tt-emp-w` установлена (210px);
  M2 ширина th = переменной ±1.5px; M3 = натуральный текст (№+5+ФИО
  192px) + 18 ±2.5px; M4 ФИО НЕ обрезаны; M5 столбцы данных РАВНЫЕ;
  M6a «Год» ≈ месячной ±3px («как на вкладке год»); M6b/M6c возврат
  на «Месяц» восстанавливает; M7 сужение при прокрутке живо;
  M8 колонка у́же прежних 42%×0.85; M9 0 JS-ошибок; P1 пиксельный
  пруф — стальная граница колонки ровно на `x = th.right`
  (rgb(64,80,102), светлая);
- десктоп 1280, обе темы: колонка месяца СКРЫТА, переменная пишется,
  0 JS-ошибок.

## Серверные шаги

НЕТ (чистый клиентский релиз). Десктоп-зеркала синхронизирует CI
(sync-to-desktop).
