# DEPLOY — Task 306 (перенос партии Tasks 298-306 из kip8test@4fc48bc)

**Дата:** 2026-09-03 · **Репозиторий:** kip8 (боевой PWA) · **SW:** kipia-v410 → kipia-v411 (одна партия = один инкремент)

**Партия:** Tasks 298 (коды статусов Т-12/Т-13, 16 кодов, «ОТ» вместо «О», серверная валидация setManualEntry),
299 (StatusCodesInit.gs — эталон листа «Коды_статусов»; выполнен в kip8test),
302 (цвет Д7,2 = Д8 #FFF9C4),
303 (слой мероприятий И/ОБ/ПЗ: приоритет «руч > отпуск > смена > мероприятие», шаг 4.6, бейдж в углу ячейки),
304 (таб_№ хранится ТЕКСТОМ: _appendRowKeepText, warnings[], TabNumbersFix.gs),
305 (дни отпуска — код отпуска основным значением + плановая смена Д/Н по циклу бейджем в формате мероприятия),
306 (бейдж только у «сменных»; мероприятия «прогул» ПР / «примечание» *; кнопки «Обновить»+«Сформировать» объединены — тихое обновление календаря при формировании).

Перенесено: index.html (патч 38 хунков, верификация байт-в-байт по изоляционному диффу), sw.js (+комментарий партии), scripts/WorkSchedule.gs + scripts/StatusCodesInit.gs + scripts/TabNumbersFix.gs (справочные копии), tests/ (4 новых + 8 обновлённых файлов, ассерты kipia-v411). Тесты: 1433 passed / 0 failed (паритет с kip8test). Browser-check: 29/29 (scripts/task306-browser-check.py, порт 8931).

---

## Что делать пользователю

### 1. Apps Script (ОДИН бэкенд на kip8 и kip8test — если уже делали для kip8test, НЕ повторять)

Сервер `WorkSchedule.gs` менялся в этой партии (Tasks 298 / 303 / 304 / 306):

1. Откройте проект Apps Script (тот же, что у kip8test): https://script.google.com — проект развёртывания **AKfycbyt…**
2. Замените содержимое файла **WorkSchedule.gs** на боевое из репозитория: `kip8/scripts/WorkSchedule.gs`
   (в редакторе: открыть WorkSchedule.gs → выделить всё → вставить новое → Ctrl+S)
3. **Управление развертываниями →** выбрать развёртывание AKfycbyt… → **Изменить → Новая версия → Развернуть**
   (НЕ «Новое развёртывание» — иначе URL изменится и оба сайта потеряют бэкенд!)

Проверка после деплоя: в шахматке нажать «Сформировать» → «Текущий месяц» — тост «Сентябрь 2026: сформировано N зап…»
без ошибок; мероприятия «прогул»/«примечание» сохраняются без «invalid_тип».

Если уже разворачивали после Task 298/303/304/306 для kip8test — делать ничего не нужно (бэкенд общий).

### 2. Листы таблицы табель_КИП_ИОС — НЕ трогать

- «Коды_статусов» уже содержит 16 кодов, включая ПР #EF5350 и «*» #FFAB91 (сверено скачиванием в Task 306).
- StatusCodesInit.gs / TabNumbersFix.gs уже выполнены в kip8test — повторно НЕ запускать.
- ⚠ Примечание: цвет «Д7,2» в живом листе введён с опечаткой (#FFF9C5 вместо #FFF9C4) — косметика,
  клиент различает коды по тексту; поправить ячейку можно вручную при случае.

### 3. Фронтенд — GitHub Pages подтянет сам

1. Push уже выполнен — GitHub Pages пересоберёт сайт через **1–3 минуты**
2. Открыть https://bloknett-design.github.io/kip8/ → **Ctrl+Shift+R** ×1–2 (SW v410 → v411)
3. Проверка: «График работы» → тулбар — одна кнопка «Сформировать» (кнопки «Обновить» больше нет);
   форма «Новое мероприятие» (страница «Инструктажи») — 5 типов, включая «прогул» и «примечание»;
   у сменного работника в отпуске — маленькие бейджи плановых смен Д/Н в правом нижнем углу ячеек,
   у дневного — только код отпуска.

### 4. APK / десктоп

- kip8-desktop получит index.html автоматически (GitHub Action sync-to-desktop.yml при пуше).
- APK (TWA Bubblewrap) собирается из того же сайта — при следующей сборке возьмёт свежий index.html.

---

## Откат

- Фронтенд: `git revert <коммит-переноса>` в kip8 → push → Ctrl+Shift+R ×2 (SW вернётся к v410).
- Сервер: в Apps Script «Управление развертываниями» → AKfycbyt… → выбрать ПРЕДЫДУЩУЮ версию → Развернуть.
- Листы не менялись — откат листов не требуется.

## Что НЕ делать

- ❌ НЕ запускать statusCodesDeployAll / updateCycleDaysTemplate2 / roleMatrixCleanup (уже выполнены в kip8test)
- ❌ НЕ создавать «Новое развёртывание» в Apps Script (только «Новая версия» у существующего AKfycbyt…)
- ❌ НЕ запускать TabNumbersFix повторно (идемпотентен, но незачем)
