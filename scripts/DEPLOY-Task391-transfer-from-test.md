# DEPLOY — Task 391 — перенос из kip8test в боевой kip8

## Что вошло (заявка)

«Общая» вкладка раздела Табель учёта рабочего времени / Работники:

1. **Строки шапки — формат В СКОБКАХ** (переделка):
   - `Работников по штату 14 (2 мастера, 7 дневных, 5 сменных).`
   - ниже строчкой `Работников на текущий момент (N) ((N) мастера, (N) дневных, (N) сменных).`
     — N = АВТОПОДСЧЁТ ПО КАТЕГОРИЯМ; ведущее число в скобках —
     СУММА категорий (сходится с разбивкой); запятые вместо «;».
2. **Шрифт строк — больше и ярче**: 14px / 600 / 0.95 (было 12px / 0.75).
3. **Кнопка «Добавить работника» — в стиле сайта (акцентная)**:
   `var(--accent-blue)` — тёмная тема СИНИЙ `#4a8fc7`, светлая
   ОРАНЖЕВЫЙ `#C6613F`, белый текст; hover/active — brightness.

## Развертывание

- **Серверных шагов НЕТ** — чистое изменение клиента (index.html).
  Один бэкенд Apps Script на оба сайта не затронут.
- SW-кэш поднят до `kipia-v467` — обновление придёт при следующем
  открытии сайта (Service Worker: skipWaiting + clients.claim).
- Тесты: **3556 passed / 0 failed** (3540 + 16 `test-task391.js`).
- Браузер-чек: **20/20** (`scripts/task391-browser-check.py`, порт
  9005, localStorage без префикса — результаты идентичны kip8test).
- CI @3ccd9c6: CI Tests / Sync content to kip8-desktop / pages
  build — success (Build Desktop App — зеркалит десктоп автоматически).
- Прод проверен curl: живой SW = `kipia-v467`, все маркеры Task 391
  в живом index.html, старых формулировок — 0.

## Источник

- kip8test `ae70fc5` (Task 391, SW kipia-test-v619).
- Перенос: `../kip8test/scripts/task391-patch.py` (7/7 якорей) +
  `scripts/task391-transfer.py` (бамп тестов/SW, копия
  test-task391.js с маппингом версий, run-all.js) +
  `../kip8test/scripts/task391-adapt-tests.py` (17 правок живых
  тестов task386/388/389/390).

## Открытые серверные шаги (за пользователем, НЕ относится к Task 391)

- ~~Task 384 (Code.gs + WorkSchedule.gs updateEmployee/updateVacation +
  «Новая версия»)~~ — **выполнено пользователем** (сообщение этого
  цикла). Кнопки «Сохранить» правок карточек работают.
- Task 387 — **ОПЦИОНАЛЕН**: getStatusCodes (пропуск пустых строк) +
  StatusCodesInit.gs (канонический справочник) нужны только чтобы
  лист «Коды_статусов» сам жил в каноническом порядке; клиент
  нормализует порядок/наименования сам (см. DEPLOY Task 387).
  Если уже сделано при деплое Task 384 — повторно не нужно.
- Task 375/376 (Flowmeter) — проверить выполнение при случае.
