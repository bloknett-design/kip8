# DEPLOY — Task 381 (перенос из kip8test e2eddb0 в kip8)

## Источник

kip8test `e2eddb0` (Task 381). Перенос: `scripts/task381-patch.py`
(копия kip8test, 9 правок по якорям — все проверены в kip8/index.html
ДО запуска), дифф 90+/37− — только зоны Task 381.

## Что переносится (только клиент — серверной части НЕТ)

1. **Фон итогов «как до белого»** — правила Task 378 удалены
   (`[light] td.ws-tt-num #FFFFFF`, `[dark] #eef0f2+#141413+
   brightness(0.88)`, `[dark] Часы #1d7a37`/`Переработка #a06a13`),
   ячейки значений итогов прозрачные (фон панели + зебра), **зебра
   чётных строк Task 322 восстановлена** (rgba(255,255,255,0.09) /
   rgba(0,0,0,0.07)). Линии/нули→пустые/перекрестье Task 378 и
   шахматка #FFFFFF Task 379 — живы.
2. **Значки раскрытия окон «Мероприятия»/«Нормы» приколоты** — при
   прокрутке текста в свёрнутом окне остаются на месте:
   `translateY(el.scrollTop)` на каждый scroll (слушатель один раз,
   флаг `_barExpPin`), transform восстанавливается при (пере)создании
   значка. Раскрытие Task 378/379 живо.
3. **Окно «Мероприятия» — общий фон окна** вместо плашек по строкам:
   строка до краёв окна (padding 3px 10px + margin 0 -10px), соседние
   строки одного срока склеены (`gap: 0` + `margin-top: 0`) — сплошная
   закраска, край цвета только на стыке прошедших↔идущих-будущих.
   Тинты Task 380 и окно «Нормы» не тронуты.

## Версии и тесты

- SW: `kipia-v457` → `kipia-v458` (guard `v459`;
  `scripts/task381-bump-sw.py` — 70 тест-файлов).
- Тесты: **3270/0** с первого прогона (+19 test-task381.js с
  маппингом `kipia-test-v610→kipia-v458` / `v611→v459`; адаптации
  test-task378/379/322/321/380 — проверено: после нормализации версий
  файлы идентичны kip8test).
- Браузер `scripts/task381-browser-check.py` **63/63** (порт 8991,
  localStorage БЕЗ префикса — `kip8_session_token`/`app-theme`; обе
  темы + мобайл 375; 0 JS-ошибок): computed прозрачность/зебра/цвета/
  линии, склейка строк ≤0.75px/краяд/стык зон, значок на месте
  (120px/колесо/конец/возврат + `_barExpPin` обоих окон), раскрытие +
  маржа 95−H, пиксели идентичны kip8test (past (245,244,240)/
  upcoming (204,203,195) на окне (233,231,222); итоги нечёт
  (233,231,222)/чёт (216,214,206) — НЕ белый).

## Развёртывание

- GitHub Pages задеплоится пушем в main (CI: Tests → Pages → sync).
- Серверных шагов НЕТ.
- Десктоп-зеркала синхронизирует CI (sync-to-desktop).
