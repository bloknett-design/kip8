# DEPLOY — Tasks 371–374: перенос пакета из kip8test в kip8

## Заявка пользователя

> «…И переноси все изменения в боевой kip8.» (Task 374; первая часть
> заявки — удаление кнопки «Копировать» — выполнена в kip8test
> коммитом 018a999 и входит в этот пакет.)

## Состав пакета (разработка в kip8test, переносится целиком)

| Задача | Что сделано | kip8test |
|---|---|---|
| 371 | «Датчики температуры»: блок «Расчёт произвольных значений» (живой двусторонний пересчёт t ↔ R(t)/E(t); обратная задача — бисекция по монотонной НСХ; диапазоны НСХ с тостами «вне диапазона») | 1693706 |
| 372 | Раздел переделан: страница ВЫБОРА = карточки всех датчиков (2 группы: ТС 8, ТП 8; бейджи, R₀·α/электроды, НСХ, ГОСТ); клик → страница датчика #page-temp-sensor-view (диапазон + шаг + таблица + справка ПО ТИПУ); каталог getTempSensorCatalog() — единый источник; состояние tempSensorKey; крошки десктопа с именем датчика; реестры страниц | fb920ee |
| 373 | Мобильные карточки столбиком на всю ширину (отступы 12px); избранное TempFav в обоих версиях (табы «Все 17 / Избранные N», звёзды на карточках и в шапке, localStorage kip8_temp_fav_v1); бейджи ТС/ТП убраны из карточек и чипа; термопара ТХК (L) хромель-копель ГОСТ Р 8.585-2001 (−200…800 °C, полиномы по таблице стандарта); панель «Расчёт произвольных значений» — статичная НАД формой диапазона/шага | c75739f |
| 374 | Кнопка «Копировать» над таблицами значений убрана (обе ветки ТС/ТП); соседние разделы (Шкала-сигнал, Буй, Весоизмерительные) не тронуты | 018a999 |

Автокоммиты Google Sheets (data/) НЕ переносились — у kip8 свой автосинк данных.

## Что перенесено

- `index.html` — через `scripts/prepare-kip8-transfer.py` (де-изоляция:
  блок isolateLocalStorage удалён, ключи `kip8test_*` → `kip8_*`,
  5 комментариев-изолятов). Дифф с kip8 = 562 строки, все ханки —
  зоны датчиков температуры (CSS .ts-*: строки ~1146–1209; разметка
  страниц ~16874–16934; реестры navigateTo/PAGE_PARENTS/PAGE_LABELS/
  топ-бар/role-access-списки; JS-блок ~30006–30533: каталог, TempFav,
  openTempSensor, calcTempSensor, справка; саблейбл меню). Проверено
  пофрагментно.
- `sw.js` — `CACHE_VERSION`: `kipia-v447` → `kipia-v451`
  (v448=371, v449=372, v450=373, v451=374).
- `tests/`:
  - новые `test-task371.js`–`test-task374.js` (110 тестов пакета) —
    версионный маппинг kipia-test-v603→kipia-v451, guard v604→v452;
  - `test-role-access.js` — адаптированная версия (temp-sensors →
    страница карточек, расчёт — на temp-sensor-view; других отличий
    от kip8 нет — проверено диффом);
  - `run-all.js` — +4 require (комментарии kip8 сохранены);
  - базовый бамп 60 файлов: ассерты v447→v451 (236), guard-ы
    v448→v452 (44); исторические v441…v446 не тронуты.
- `scripts/task371-374-port-tests.py`, `scripts/task371-374-browser-check.py`,
  скриншот-пруфы.

## Тесты

`node tests/run-all.js` → **3096 passed / 0 failed** с первого прогона
(2986 + 110 тестов пакета).

## Браузерная проверка (порт 8976, localStorage БЕЗ префикса)

36/36: карточки 8+9 (ТХК (L) хромель-копель −200…800); табы
«Все 17 / Избранные 0»; бейджей на карточках нет; звёзды 17; клик ☆ → ★,
«Избранные 1», фильтр таба, пустая группа скрыта; ТХК (L) — панель
НАД формой (видна сразу), подпись «Термо-ЭДС», живой расчёт
E(100)=6,862, звезда в шапке; «Рассчитать» — таблица E(t) БЕЗ кнопки
«Копировать» (кнопок 0), E(100)=6,8617; 50М — R(t), R(50)=60,7, без
кнопки; «Шкала-сигнал» — своя кнопка на месте; светлая — ТХА (K)
без кнопки, E(100)=4,096; мобайл 375 — карточки столбиком на всю
ширину; 0 JS-ошибок во всех 3 контекстах. VLM по 3 скриншотам:
таблицы без «Копировать», карточки столбиком.

## Серверные действия

**НЕ ТРЕБУЮТСЯ.** «Датчики температуры» — чистый клиентский
калькулятор; Apps Script не менялся ни в одной задаче пакета.

## Откат

```bash
git revert <commit>   # index.html + sw.js + tests (вернёт kipia-v447)
```
