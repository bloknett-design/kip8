# DEPLOY Task 365 — ПЕРЕНОС из kip8test в kip8 (боевой)

**Дата:** 2026-09-13
**Заявка пользователя (Task 365, с поправкой):** «В разделе Расходомеры хозрасчётные, в Хозрасчёте №12
период указан еженедельно, но фактически нужно ежедневно, в соответствии с формой ввода данных… значения
введённых расходомеров должны быть красными с шести утра до семи утра…» → поправка: «**не ровно час** —
красными с 6:00 новых суток (следующих за сутками ввода данных), пока не введут новые данные».
**Заявка переноса:** «Перенос в боевой kip8».

## Что переносится (проверено пользователем в kip8test, коммит `4a22d39`)

| Пункт | Суть | kip8test-коммит | SW kip8 |
|---|---|---|---|
| 365-1 | **№12 «Ежедневно»**: нормализация периода на клиенте во всех 3 точках наполнения `_METERS` (fallback-json `data/flowmeters.json` / кэш localStorage `_restoreCache` / сервер `flowmeter.list` в `load`) — новый метод `FlowmeterData._normalizeMeters`; fallback-json №12 тоже «Ежедневно». Таблица (Google Sheets, лист `hozraschet_meters`) хранит устаревший «Еженедельно» — нормализация идемпотентна, когда таблицу поправят, ничего не изменится | `4a22d39` | v442 |
| 365-2 | **Красный «пора вводить» — НЕ час**: с 6:00 НОВЫХ суток (следующих за сутками ввода данных) держится до ввода новых данных (7:00, 12:00, следующие дни — всё красный); зелёный возвращается сразу после сохранения новых показаний и держится до 6:00 следующих суток. Поведение Task 357 подтверждено поправкой, закреплено в комментариях `_isOverdue`/CSS и тестах | `4a22d39` | v442 |

## Как переносилось

- `prepare-kip8-transfer.py` (де-изоляция kip8test/index.html при `4a22d39`) →
  дифф с kip8/index.html (35eee49) = **ровно зона Task 365** (41 строка,
  проверено пофрагментно: комментарий CSS `.flow-summary-val-due`, 3 точки
  применения + определение `_normalizeMeters`, комментарий-поправка в блоке
  `_isOverdue`). Косметика комментариев Task 364 («3px»→«2px») уже была в kip8
  (перенос 364 шёл из рабочего дерева с хвостом).
- `scripts/task365-port.py`: cp index.html; data/flowmeters.json №12
  «Еженедельно»→«Ежедневно» (+\n в конце файла); SW-бамп
  **`kipia-v441` → `kipia-v442`** (guards СНАЧАЛА v442→v443, затем ассерты
  v441→v442 — урок Task 361; 54 тест-файла).
- Тесты: `tests/test-task365.js` перенесён с маппингом версий
  (kipia-test-v594→v442, v593→v441, v595→v443); `run-all.js` +1 require.
  Прочие изменения тестов в kip8test-коммите были только версиями SW —
  покрыты бампом kip8.

## Проверки kip8 после переноса

- **Тесты: 2838 passed / 0 failed** (было 2806; +32: `test-task365.js`
  — `_normalizeMeters` (№12 из «Еженедельно»/«Ежемесячно»/пусто, идемпотентность,
  другие не тронуты, не-массив/null), таймлайн красного (зелёный до 6:00
  следующих суток, красный с 6:00 и ВНУТРИ часа, и ПОСЛЕ 7:00/12:00/23:59,
  днями спустя; зелёный сразу после ввода, новый цикл в 6:00), интеграция №12
  (недельная запись → суточный ритм; №3 недельный не тронут), SRC (3 точки,
  комментарии-маркеры), JSON, SW v442/guard v443).
- **Браузер 24/24** (`scripts/task365-browser-check.py`, порт 8964, моки
  сервера + кэш localStorage с «Еженедельно» у №12, патч даты subclass-Date →
  `window.__fixed`): десктоп 1280 тёмная — бейдж №12 «Ежедневно» (нормализация),
  №3 «Еженедельно» не тронут; №2/№12 зелёные (введено сегодня за вчера), №4
  красный; завтра 6:30 — №2 и №12 КРАСНЫЕ; 7:30/12:00/послезавтра — красный
  держится (НЕ час); деталь №12 «Ежедневно»; форма ввода — дата по умолчанию
  «вчера», сразу после «Сохранить» №2 ЗЕЛЁНЫЙ (оптимистично) и остаётся
  зелёным после серверного `load()`; №12 без данных — красный; мобайл 375 —
  жив, бейдж и цвета на месте; 0 JS-ошибок.
- **VLM-пруф** (`scripts/task365-proof-desktop.png` + `…-form.png`):
  бейдж №12 «Ежедневно» — ДА; №3 «Еженедельно» — ДА; №4 красный, №2/№12
  зелёные — ДА; форма ввода с датой «вчера» и кнопкой «Сохранить» — ДА.

## Серверная часть — НЕ ТРЕБУЕТСЯ

Логика Task 365 полностью клиентская (нормализация на клиенте). Apps Script
(`Flowmeter.gs`) не менялся. Период в Google Sheets (`hozraschet_meters`,
№12 «Еженедельно») можно поправить вручную в любое время — нормализация
идемпотентна, поведение не изменится.

## Откат

```
git -C /home/z/my-project/kip8 revert <коммит Task 365>
```

index.html/sw.js/data/flowmeters.json/tests вернутся к состоянию пакета
360–364 (kipia-v441). Кэш пользователей обновится при следующей загрузке
после того, как SW увидит новую версию (2-фазное обновление).

## Коммиты

- kip8test: `4a22d39` — Task 365 (исходник переноса, kipia-test-v594)
- kip8: `c712552` — Task 365 перенос (kipia-v442)

## ⚠️ PUSH НЕ ВЫПОЛНЕН — PAT отклонён GitHub

Push коммита `c712552` в `bloknett-design/kip8` не выполнен: PAT из
последнего сообщения пользователя (действующий с 2026-08-24) отклоняется
GitHub на всех эндпоинтах — API `GET /user` → **401 «Bad credentials»**,
`git push` → **401 «Invalid username or token»** (на оба репо, все схемы:
Bearer/token/-u, username x-access-token/oauth2/аккаунт). Чтение репо
работает только потому, что они публичные (анонимный доступ). Ранее
рабочий токен из `/home/z/.kip_pat` был перезаписан новым по инструкции
сессии и не восстановим (оверлей-ФС контейнера, бэкапов нет).

**Действие после получения валидного PAT (Contents: Read and write):**

```
TOKEN=<новый PAT>
git -C /home/z/my-project/kip8 push "https://x-access-token:${TOKEN}@github.com/bloknett-design/kip8.git" main
printf '%s' "$TOKEN" > /home/z/.kip_pat && chmod 600 /home/z/.kip_pat
```

После push: проверить CI (CI Tests, Deploy Pages, sync-to-desktop), убедиться
в живом SW kipia-v442 на https://bloknett-design.github.io/kip8/sw.js.
