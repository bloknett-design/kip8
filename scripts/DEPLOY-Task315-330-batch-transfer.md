# Task 315-330 — ПЕРЕНОС ПАРТИИ из kip8test@f9e1a3c в боевой kip8 (один инкремент SW kipia-v417→v418)

**Дата:** 2026-09-06
**Заявка пользователя:** «…перенеси все последние изменения в боевой kip8
и kip8-desktop» (партия Tasks 315–330, заявки по каждой — в worklog kip8test
и DEPLOY-доках kip8test).

**Состав партии (16 задач):**
- **315** — бар над шахматкой из ТРЁХ равных частей (колонка кнопок /
  окно всех мероприятий месяца / окно норм и праздников), перенос текста
  в окнах, кнопка «Отменить», строки 2/3 тулбара;
- **316** — наведение на дату шапки → подсветка ВСЕГО столбца, клик →
  фильтр окна мероприятий по выбранному дню (тоггл/Esc/сброс);
- **317** — «данные от …» в тултип кнопки «Обновить»; кнопки в 3 ряда
  ровно в высоту окон (95px), зазоры 3px;
- **318** — увольнение сотрудника (дата_увольнения H + в_архиве=1 I;
  сервер workSchedule.dismissEmployee);
- **319-325** — попап кодов (мелкий шрифт/перенос), карточка сотрудника,
  шапка сетки, стрелки выбора, часы д/н, колонка ФИО по самому широкому
  тексту, зебра и тонкие линии шторки итогов;
- **326** — BUGFIX: не открывались разделы и полные карточки расходомеров
  (потерян закрывающий `</div>` #page-work-schedule в Task 323);
- **327** — «Итоги учёта»: без строки Итого/столбца Всего; Явки/Часы/
  Переработка; доп. столбцы за кнопкой «Ещё» (порядок по заявке);
  равные столбцы; заголовки в 2 строки; бордюрчик; вкладки при открытии;
- **328** — кнопка-тумблер перекрёстной подсветки (слева от
  «Сформировать», localStorage, aria); кнопки бара меньше; тултипы
  убраны; подсказка «Сформировать» в формате «Обновить»;
- **329** — шторка: «Ещё» → ШЕВРОН на левом краю (середина высоты) +
  бортик 5px; ширина РОВНО по столбцам (узкие, заголовки в 2 строки,
  130/514px); ПЕРЕРАБОТКА В ДНЯХ (часы в тултипе); зебра колонки ФИО;
- **330** — шапка шахматки сине-серая (#1e293b тёмная / #bfcad5 светлая,
  hover #2a3a4c); попап кодов — описания 10px; светлая тема — весь текст
  окон «Мероприятия»/«Норма» ЧЁРНЫЙ (подложки цветные).

## Что перенесено

1. **index.html** — патч 3950 строк (diff kip8test 894afab..f9e1a3c,
   `git apply` чисто; изоляционных строк в диффе 0 — kip8 остался БЕЗ
   isolateLocalStorage, проверено: 0 вхождений; 44 175 строк).
2. **sw.js** — `kipia-v417` → `kipia-v418` (один инкремент партии).
3. **СЕРВЕРНАЯ ЧАСТЬ партии** (впервые с Task 306): scripts/Code.gs
   (+5 строк: case `workSchedule.dismissEmployee`) и
   scripts/WorkSchedule.gs (+336 строк: `dismissEmployee` и пр.).
   **Apps Script развёртывание НЕ требуется** — бэкенд общий для
   kip8/kip8test (WEB_APP_URL один и тот же), код уже развёрнут в
   сессиях kip8test; в репозитории kip8 файлы .gs синхронизированы.
4. **tests/** — все 48 файлов из kip8test (16 новых test-task315…330 +
   32 обновлённых копии) с адаптацией версий:
   `kipia-test-v569 → kipia-v418` (текущая), `v570 → v419` (сторож),
   исторические `v5XX → v417`; run-all.js — полная регистрация.
5. **Доки** — browser-check скрипты партии (порты 8980-8997) +
   DEPLY-Task315-330-batch-transfer.md (этот файл) +
   task315-330-transfer-update-prompt.py (промт post-партия).

## Верификация

- **Тесты:** `node tests/run-all.js` — **2010 passed / 0 failed**
  (паритет с kip8test@f9e1a3c; 1584 → 2010).
- **node --check:** 4 script-блока index.html — OK.
- **Браузер на РЕАЛЬНОМ kip8/index.html** (13 скриптов, 420 проверок):
  task315 33/33, task316 28/28, task317 49/49, task318 45/45,
  task319 48/48, task320 24/24, task327 30/30, task328 36/36,
  task329 43/43, task330 27/27 + регресс task312 31/31, task313 30/30,
  task314 36/36. Скрипты 318/319 актуализированы под Task 330
  (шапка/hover/10px). Скрипты 321-325 — устаревшие ДО Task 329
  (падают одинаково в kip8test и kip8; superseded 327/329 — задокументи-
  ровано в worklog kip8test@Task 329).
- **Промт/счётчики:** версия v418, тесты 2010/48, ~44.2 тыс. строк.

## Деплой пользователя

1. GitHub Pages — подхватит сам после push (1-3 мин).
2. **Ctrl+Shift+R ×1–2** (SW kipia-v418).
3. Apps Script/листы — **НЕ трогать** (серверная часть уже развёрнута
   с сессий kip8test — бэкенд общий).
4. kip8-desktop — автосинк `sync-to-desktop.yml` (проверить коммит
   «auto: sync content from kip8@…» после пуша).
