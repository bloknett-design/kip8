# DEPLOY Task 360–364 — ПЕРЕНОС ПАКЕТА из kip8test в kip8 (боевой)

**Дата:** 2026-09-10
**Заявка пользователя (Task 364):** «Красные линии вокруг ГРУППЫ ячеек выходных сделай 2px и по меньше
яркости. Коды под графиком на печати размести в столбик справа от столбика мероприятий.
**И перенеси в боевой kip8.**»

## Перенесённый пакет (5 задач, проверены пользователем в kip8test)

| Задача | Суть | kip8test-коммит | SW kip8 |
|---|---|---|---|
| 360 | Печать табеля: под шахматкой — список мероприятий месяца (столбик: дата · код · тема · ФИО) и перечень кодов ТОЛЬКО этого месяца с наименованиями (столбик) | `2f845e3`-поколение | v437 |
| 361 | Печать: колонка «Сотрудник» по ширине текста (canvas measureText, кламп 24–48mm, inline width), дни делят освободившуюся ширину поровну; все шрифты листа увеличены (база 11px и т.д.); бейджи мероприятий в ячейках ВЕРНУТЫ (сплошные/пунктирные/виртуальные) | `2b3064a` | v438 |
| 362 | Печать: в видах «сменный»/«дневной» мероприятия — только сотрудников печатаемых строк; коды мероприятий (И/ОБ/ПЗ/ПР/*) попадают в перечень кодов (баг «ПЗ — Проверка знаний» у дневного персонала) | `2f845e3` | v439 |
| 363 | Экранная сетка: фон обычных выходных = пустым ячейкам; красная рамка вокруг ГРУПП столбцов выходных (включая шапку с датами и низ, внутренняя грань 1px); праздники — розовый #f8e2e9 + та же заливка ячеек дат в шапке | `8a113d3` | v440 |
| 364 | Рамка-группа выходных 3px #e53935 → **2px #e57373** (тоньше и бледнее); на печати коды — **столбиком СПРАВА от столбика мероприятий** (обёртка wsp-bottom: мероприятия слева flex:1, коды справа max-width 44%), сноска — под обоими на всю ширину | `c0d9458` | **v441** |

## Как переносилось

- `prepare-kip8-transfer.py` (де-изоляция kip8test/index.html: блок
  isolateLocalStorage, префиксы ключей localStorage, комментарии-изоляты) →
  дифф с kip8/index.html = **ровно зоны Tasks 360–364** (705 строк, проверено
  пофрагментно: print CSS, шрифты/бейджи/ширина «Сотрудника», сетка wgrp/feast,
  JS _buildPrintHtml/_printCell/_renderGrid/_renderCell/хелперы _calDayFeast/
  _calWend). Кнопка увольнения `.ws-dismiss-submit` (#e53935) не тронута.
- `scripts/task360-364-port-tests.py`: перенос тестов с маппингом версий
  (kipia-test-v593→v441, v594→v442, префикс «нет захардкоженных версий»
  kipia-test-v59→kipia-v44). Перенесены/адаптированы 11 файлов:
  test-task360–364.js (новые), test-task319/341/342/343/355/work-schedule.js
  (правки Tasks 361–364 к старым тестам). run-all.js: +5 requires.
- SW kip8: `kipia-v436` → **`kipia-v441`** (бамп-скрипт: 49 тест-файлов,
  guards v437→v442). Кэш картинок не зависит от версии (пользователи
  не перекачивают). Обновление придёт при следующей загрузке (2 фазы SW).

## Проверки kip8 после переноса

- **Тесты: 2806 passed / 0 failed** (было 2688; +118: новые task360–364
  и адаптации старых под новые реалии).
- **Браузер 24/24** (`scripts/task360-364-browser-check.py`, порт 8962):
  сетка (2px #e57373 бока th/td + inset-тень верха + низ, грань Сб|Вс 1px,
  фон выходного == пустому, праздник розовый + дата, статусная «д»
  inline-фон); печать полного вида (wsp-bottom flex/row, коды ПРАВЕЕ
  мероприятий по rect, ширина кодов ≤ 44% ряда, сноска под обоими на всю
  ширину, «ПЗ — Проверка знаний» в перечне, 3 мероприятия слева); мобайл 375
  жив; 0 JS-ошибок.
- **VLM-пруф** (`scripts/task360-364-proof-print.png` + `…-grid.png`):
  мероприятия слева/коды справа в один ряд — ДА; ПЗ в перечне — ДА; тонкая
  спокойная красная рамка вокруг групп выходных — ДА.

## НЕ переносилось (не относится к пакету)

- GAS-сервер: не менялся (Tasks 360–364 — только клиент).
- electron/main.js: изменений в пакете нет, десктоп синхронизируется CI
  (sync-to-desktop).
- Тест-изоляты kip8test (префикс localStorage, ключи кешей) — остались
  только в kip8test, в kip8 их никогда не было.

## Откат (если понадобится)

`git revert` коммита переноса в kip8 вернёт index.html/tests/sw.js к
kipia-v436 (Task 359). Серверная часть не затронута, миграций нет.
