# Task 401–404 — перенос из kip8test в kip8 (Табель: годовая шторка итогов; тип третьей строкой + группа допуска; «р.»/попап без СИЗ/фикс комментария/ярлыки; три колонки карточки)

**Перенос задач 401–404** (разработаны и приняты в kip8test):
коммиты kip8test 0f39a94 (401) → 9dfd4b6 (402) → cae2b6c (403) →
a038911 (404).

**Репозиторий**: kip8 (боевой сайт)
<https://bloknett-design.github.io/kip8/>

**Версия SW**: kipia-v475 → **v476**
**Тесты**: 3823 passed / 0 failed (было 3732/0; +91: test-task401 ×23,
test-task402 ×27, test-task403 ×29, test-task404 ×12; адаптации 12
файлов, перенесены из kip8test с маппингом версий)
**Браузер-чек**: 54/54 ×3 контекста (порт 9008, ключи localStorage без
префикса), 0 JS-ошибок; VLM ×1 (годовая шторка) — подтверждено.

## Что перенесено

### Task 401 — окно итогов «Год» (десктоп)
- Шторка итогов года раскрывается на ВСЮ ШИРИНУ правее колонки ФИО
  (вплотную к столбцу фамилий; шахматка полностью закрыта).
- Шапка годовой таблицы — двухстрочная: подстрока-пояснение
  «дней/часов» под числами месяцев (colspan 12).
- Три итоговых столбца (Дней/Часов/Перераб. (дни)) — фон другого
  оттенка + вертикальный разделитель слева.

### Task 402 — тип работника + столбец «группа_допуска»
- Ячейка сетки — ТРИ строки: ФИО → должность → тип («смена №1» /
  «сменный» / «дневной»), шрифт/высота должности.
- «группа_допуска» (лист «Сотрудники», столбец по заголовку): в
  ячейке после должности, в карточке, сводной «Общая», селект шторки
  правки → addEmployee/updateEmployee.
- GAS: `_accessGroupColIndex`, guard по payload.

### Task 403 — «разряд» → «р.»; попап без СИЗ; фикс бага комментария
- Столбец ФИО: должность со сокращением «разряд» → «р.» («Слесарь
  КИПиА 5 р.»), группа допуска убрана из столбца (жива в
  карточке/сводной/шторке). Печать — полная должность.
- Попап по клику на ячейку с фамилией — БЕЗ блока СИЗ.
- **ФИКС БАГА КОММЕНТАРИЯ (GAS)**: столбец «группа_допуска» в листе
  «Сотрудники» пользователя стоит МЕЖДУ «должность» (J) и
  «комментарий» (K) — жёсткие индексы читали/писали не туда.
  Теперь «должность»/«комментарий» тоже по заголовкам строки 1
  (`_headerColIndex`, фолбэк J/K) — каждый реквизит в свой столбец.
- Ярлыки «Работников» — ширина по самому длинному тексту
  (`--ws-wtabs-w`, фолбэк 236px).

### Task 404 — три блока в одну линию; кнопки рядом; окно без отпусков
- Карточка «Работники»: ТРИ колонки — профиль (+отпуска под ним) |
  СИЗ | мероприятия (СИЗ — слева от мероприятий, три верхних блока
  в одну линию, равные доли).
- Кнопки ✎/✕ в блоках отпуска и мероприятия — РЯДОМ у правого
  края, как в СИЗ (контент строк растянут `flex: 1`).
- Окно «Мероприятия» тулбара — секция «Отпуска» УДАЛЕНА (остались
  «Мероприятия» и «СИЗ»).

## ⚠ СЕРВЕРНЫЙ ШАГ (у пользователя)

Обновить `WorkSchedule.gs` в Google Apps Script проекта табель_КИП_ИОС
(скопировать `scripts/WorkSchedule.gs` из репозитория — изменения
задач 402/403: `_accessGroupColIndex`/`_headerColIndex`,
listEmployees/updateEmployee/addEmployee). До деплоя: «группа_допуска»
не читается/не сохраняется и баг комментария остаётся (комментарий в
поле показывает группу допуска); фронтенд работает без ошибок.

## Проверки

- Тесты: `task401-404-transfer.py` — патчи 401/402/403/404 к
  index.html и WorkSchedule.gs (базы идентичны: diff только
  localStorage-ключи/комментарии, вне зон патчей; GS совпал побайтово);
  16 контентно-изменённых тестов скопированы с маппингом
  kipia-test-v631 → kipia-v475 / v632 → v476 (исторические guards
  kipia-test-v5XX общие — не тронуты); run-all.js — точечная регистрация
  401–404 (в kip8 есть kip8-специфичный test-task344.js — файл не
  копировался); `task401-404-bump-sw.py` v475 → v476 (двухшаговый:
  guards v476→v477, ассерты v475→v476). Итог: 3823/0.
- Верификация идентичности: diff kip8/index.html ↔ kip8test — те же
  82 строки репо-специфики, что и до переноса (коды задач совпали
  полностью); WorkSchedule.gs — 0 различий.
- `task401-404-browser-check.py` 54/54 (порт 9008; ключи kip8 без
  префикса): 401 — ws-tt-yearfull, ширина шторки = рабочая область −
  ФИО (±4px), примыкание к столбцу ФИО (±3px), двухстрочная шапка
  «дней/часов»; 402/403 — «Слесарь КИПиА 5 р.» без группы, тип третьей
  строкой, окно мероприятий без «Отпуска ·» (СИЗ жив), попап без СИЗ;
  404 — ТРИ колонки в одну линию, СИЗ левее мероприятий, кнопки ✎/✕
  рядом (зазор ≤ 12px, прижаты вправо), правка комментария →
  updateEmployee (группа отдельно); мобайл — стек профиль → отпуска →
  СИЗ → мероприятия. 0 JS-ошибок ×3.
- VLM ×1 (glm-5v-turbo): годовая шторка (тёмная) — подстрока
  «дней/часов» под месяцами, итоговые столбцы выделены фоном и
  разделены вертикальной линией, сетка закрыта.

## Серверные шаги

1. ⚠ Обновить WorkSchedule.gs в Apps Script (см. выше) — пользователь.
