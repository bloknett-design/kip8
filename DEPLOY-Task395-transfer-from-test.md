# Task 395 — перенос из kip8test в боевой kip8

**Дата:** 23.09.2026
**Репо:** kip8 (боевой сайт)
**SW:** kipia-v470 → **kipia-v471**
**Источник:** kip8test коммит febce3e (Task 395)

## Что перенесено

Полный функционал Task 395 (см. `DEPLOY-Task395-workers-matrix-bg-columns.md`
в kip8test):

1. **Кнопка «Работники» — матрица доступа ролей** — не
   отображается пользователям без доступа к разделу «Табель учёта
   рабочего времени» (null) и с ограниченным просмотром (min);
   edit/view — видна (у view страница read-only). Гейты перехода и
   прямого URL — те же правила.
2. **Фон и рамки блоков карточек** — фон не сливается с общим
   фоном окна (тёмная #243349, светлая #ffffff), рамки 2px ярче,
   стиль «выступающего вверх бордюрчика» (светлая верхняя кромка +
   тень снизу).
3. **Десктоп-раскладка карточки (≥1024px)** — ДВЕ равные колонки:
   отпуска ПОД профилем, мероприятия ПОД отпусками (лево), СИЗ —
   ВЕРХНЯЯ ПРАВАЯ часть экрана (право). Мобайл — вертикальный стек.

## Изменённые файлы

- `index.html` — зеркально kip8test (7 правок);
- `sw.js` — kipia-v471;
- `tests/test-task395.js` (+18, SW-версии замаплены), адаптация
  test-task311/385/394, бамп SW-ассертов (84 файла),
  `tests/run-all.js`;
- `scripts/task395-{patch,adapt-tests,transfer,bump-sw,
  browser-check}.py`.

## Проверки

- Тесты: **3672 passed / 0 failed** (3654 + 18).
- Браузер: **31/31** на порту 9007 (5 контекстов ролей; localStorage
  без префикса репо) — пруфы `task395k8-proof-*.png`.

## Серверные шаги

**НЕТ.** Apps Script не тронут (права — из уже работающей матрицы
`getMyAccess`; серверный деплой Task 392 выполнен ранее).

## После деплоя (пользователю)

Обновить PWA у клиентов: пункт «Обновить» в меню или повторное
открытие сайта (SW kipia-v471 подхватится автоматически).
