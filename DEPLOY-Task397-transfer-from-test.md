# Task 397 — перенос из kip8test 771f014 в боевой kip8

**Дата:** 23.09.2026
**Репо:** kip8 (боевой сайт)
**SW:** kipia-v472 → **kipia-v473**
**Коммит:** feat: Task 397 (перенос из kip8test 771f014)

## Что перенесено

ОБЩЕЕ ПРАВИЛО (заявка): «если у пользователя, согласно его роли,
нет доступа к определённому разделу, то кнопка данного раздела
не должна отображаться» — на ВСЁ приложение:

1. **Универсальный проход `_applyRoleToUI`** — любой элемент с
   `onclick="navigateTo('…')"` (кроме хлебных крошек) виден ⟺
   `canAccess(цель)` (тот же гейт, что в `navigateTo`);
   симметричен при смене роли; спец-скрытия (фильтр 4,
   «Графики» вне Electron, пустые родители Task 115) не
   перебиваются.
2. **Карта JS_NAV_TARGETS** — 8 кнопок с addEventListener-onclick
   (входы КИП ИОС + «Графики») под тем же гейтом.
3. **«Инженерные калькуляторы» нижнего бара** — гейт по доступу
   (прежде «видна всегда»: у роли без calc.view — «Нет доступа»).
4. **Композит docs|library|kip-ios → docs** (нижний бар +
   вкладка «Документация» десктопа) — кнопка видна ⟺ переход
   возможен.
5. **Нижний бар скрыт целиком**, если обе кнопки недоступны.

## Перенос

- `scripts/task397-patch.py` — 3/3 правки (якори совпали
  зеркально; дифф kip8↔kip8test по index.html 82 строки —
  зоны патча не затронуты);
- `scripts/task397-adapt-tests.py` — test-role-access.js (2
  правки: docs-формула + новые проверки калькуляторов/бара);
- `scripts/task397-transfer.py` — test-task397.js (+14,
  до-бамп форма v472/v473) + run-all.js;
- `scripts/task397-bump-sw.py` — v472→v473 (guards v474
  первыми, 87 файлов; свой тест не исключён — двухшаговый бамп
  канонизирует).

## Проверки

- Тесты: **3707 passed / 0 failed**.
- Браузер: **35/35** (порт 9007, localStorage без префикса
  kip8test: — 6 контекстов: «Запрет»-уровень десктоп+мобайл /
  только-calc / Админ / ИТР ТОКЕМ-фильтр-4 / мобайл-светлая;
  клик «Инженерные калькуляторы» ведёт на страницу, НЕ «Нет
  доступа»); пруфы `task397k8-proof-*.png`.

## Серверные шаги

**НЕТ.** Apps Script не тронут.

## После деплоя (пользователю)

Обновить PWA у клиентов: пункт «Обновить» в меню или повторное
открытие сайта (SW kipia-v473 подхватится автоматически).
