# DEPLOY — Task 377: перенос из kip8test в боевой kip8

Источник: kip8test 395d5b3 (Task 377, kipia-test-v606).

## Заявки пользователя

1. «В шахматке Табель учёта рабочего времени, выделение текущего дня
   сделай посильнее.»
2. «В светлой теме, разделительные полосы ячеек шахматки такого же
   цвета, как в тёмной теме.»

## Что перенесено (только клиент, серверной части НЕТ)

- **index.html** — дифф 181 строка (161+/20−), побайтово совпадает с
  диффом Task 377 в kip8test (проверено diff-сравнением):
  - усиление «сегодня»: заливки ячеек 0.16→0.30 (тёмная) / 0.10→0.22
    (светлая), шапка 0.20→0.40 / 0.13→0.26 + число жирное (700),
    row+today 0.20→0.32 / 0.14→0.24;
  - составные правила «сегодня+hover/sel» (тело и шапка, обе темы) —
    подсветка НЕ гаснет при наведении/выборе столбца;
  - акцентная полоса 2px ::after под датой (тёмная — var(--accent-blue),
    светлая — #2a5d8f; без конфликта с красной рамкой ws-wgrp);
  - полосы светлой темы = ЦВЕТА тёмной: дни rgb(10,15,23), ФИО
    rgb(64,80,102), шапка rgb(83,96,117); тёмная тема НЕ тронута.
- **sw.js** — kipia-v453 → **kipia-v454** (scripts/task377-bump-sw.py;
  66 тест-файлов: ассерты v453→v454, guard-ы v454→v455).
- **tests/** — test-task377.js (маппинг kipia-test-v606→kipia-v454 /
  v607→v455, чистка остаточных сообщений v605/v607) + адаптации
  test-task313/319/355/363.js (новые значения). run-all.js +1 require.
- Автокоммиты данных НЕ затронуты.

## Тесты

node tests/run-all.js → **3183 passed / 0 failed** (3164 + 19 новых,
с первого прогона).

## Браузерная проверка (порт 8982, localStorage БЕЗ префикса — урок Task 365)

scripts/task377-browser-check.py — 28/28, 0 JS-ошибок:
- computed: заливки 0.30/0.22, шапки 0.40/0.26, полоса 2px, границы
  светлой темы rgb(10,15,23)/rgb(64,80,102)/rgb(83,96,117);
- hover/select столбца «сегодня» не гасит (0.34/0.26, шапка 0.44/0.30);
- красная рамка выходных ws-wgrp-first жива;
- ПИКСЕЛЬНО линии совпадают между темами: (9,15,23) тёмная /
  (10,15,23) светлая, обе ориентации;
- тинт «сегодня» виден в пикселях.

VLM-пруф (task377-proof-light.png): столбец 17 заметно выделен, линии
тёмные (почти чёрные), красные рамки выходных видны.

## Деплой

GitHub Pages (автоматически из main). После публикации живой SW:
`kipia-v454`. Серверных шагов НЕТ (чистый CSS-клиент) — вручную
обновлять Apps Script НЕ требуется.

## Откат

git revert коммита переноса.
