# DEPLOY — Task 394: перенос из kip8test в боевой kip8

**Дата:** 2026-09-22
**Репозиторий:** kip8 (боевой PWA) — перенос коммита kip8test `8d11ff7`
**SW:** kipia-v469 → **kipia-v470**
**Заявка:** сетка 2×2 карточки работника на весь экран (отпуска справа профиля,
СИЗ под отпусками) + мероприятия за весь год в карточках + окно мероприятий
месяца с отпусками (частично на два месяца — в обоих) и СИЗ.

**Статус серверных шагов:** серверный деплой Task 392 (Code.gs + WorkSchedule.gs +
PPEInit.gs → ppeDeployAll → «Новая версия») ВЫПОЛНЕН пользователем — раздел «СИЗ»
работает. Task 394 серверных шагов НЕ имеет (см. ниже).

---

## ЧТО ПЕРЕНЕСЕНО

Полный зеркальный перенос правок Task 394 (детали и проверки —
DEPLOY-Task394-workers-grid-year-events-window.md в kip8test):

1. **Десктоп (≥1024px), страница «Работники»**: четыре блока карточки —
   СЕТКА 2×2 на весь экран (обёртка `.ws-wgrid2`, равные колонки 1fr 1fr,
   зазор 12px): профиль слева сверху, **отпуска — справа от профиля**,
   мероприятия — под профилем, **СИЗ — под отпусками**; кап ширины 1020px
   снят; «Общая» — без сетки; мобильный (≤1023px) — прежний вертикальный стек.
2. **В карточках работников мероприятия — НА ВЕСЬ ГОД**: `_loadTrainings`
   запрашивает `{ year }` без месяца (сервер отдаёт все записи года — исходная
   способность, Apps Script не менялся); блок 3 — «Мероприятия · 2026»,
   колонка «Общей» вкладки — тоже год.
3. **Окно мероприятий месяца (тулбар табеля)**: новые секции «Отпуска ·
   месяц · N» (периоды, пересекающие месяц; **частично попадающие на два
   месяца — в окнах ОБЕИХ месяцев**) и «СИЗ · месяц · N» (записи, у которых
   срок годности истекает в месяце; «До износа» скрыто); режим выбранного
   дня (Task 316) — накрывающие отпуска и истекающие ровно в день СИЗ;
   прошедшие строки — прозрачный фон (Task 380/389).

## ПЕРЕНОС
- `scripts/task394-patch.py` — 11/11 якорей с первого прохода (index.html
  kip8 побайтово совпадает в зоне правок);
- `scripts/task394-adapt-tests.py` — 4 правки (тесты 393/309: заголовок
  «· 2026», «нет мероприятий за год»);
- `scripts/task394-transfer.py` — копия test-task394.js с маппингом
  SW-версий (ассерт kipia-v470, guard kipia-v471), регистрация в run-all.js,
  SW-бамп v469→v470 (guards v470→v471 первыми, 83 файла).

## СЕРВЕРНЫЕ ШАГИ — НЕТ
Apps Script не трогался: `listTrainings` без `month` умеет отдавать весь год
С САМОГО НАЧАЛА («если month не указан — все мероприятия года» — исходный код
сервера; текущий деплой Task 392 его включает). «Новая версия» НЕ требуется.
Один бэкенд обслуживает оба сайта — изменений нет ни для kip8, ни для kip8test.

## ПРОВЕРКИ
- Тесты: **3654 passed / 0 failed** (3648 из kip8test + 6 kip8-специфичных
  test-task344);
- Браузер-чек **25/25** (порт 9007, localStorage без префикса; десктоп тёмная
  1280 + мобайл 375 светлая): сетка 2×2 (отпуска справа профиля, СИЗ под
  отпусками, равные колонки, весь экран), «Мероприятия · 2026» с записями
  всего года, окно мероприятий: отпуск 29.09–02.10 — В ОБОИХ месяцах, СИЗ
  «до 12.09»/«до 05.10», день 12, «Общая» — колонка-год; 0 JS-ошибок ×2;
- CI ×4 success (Tests / Sync content to kip8-desktop / Build Desktop App /
  pages build and deployment);
- ПРОД kip8: SW **kipia-v470**, все маркеры Task 394 в живом index.html
  (ws-wgrid2 ×5, годовой listTrainings, «нет мероприятий за год»,
  ws-ep-cap-vac/ppe, кап 1020px = 0).

**Коммит:** b371d5e · **Пруфы:** task394-proof-workers.png,
task394-proof-events.png, task394-proof-mobile.png (в kip8)

## КАК ОБНОВИТЬСЯ ПОЛЬЗОВАТЕЛЮ
PWA подхватит новую версию сам (SW kipia-v470); при открытом приложении —
Ctrl+Shift+R ×1–2. Данные сервера/листы не менялись.
