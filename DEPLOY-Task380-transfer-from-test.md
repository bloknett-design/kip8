# DEPLOY — Task 380: перенос из kip8test в kip8

## Источник

kip8test `33dd957` (Task 380 — e49a93f + chore-синхронизация патч-скрипта).
Дифф kip8/index.html — 68 вставок / 1 удаление — ТОЛЬКО зоны Task 380
(побайтово воспроизводится scripts/task380-patch.py из базы Task 379).

## Что перенесено

1. **Окно «Мероприятия» бара — фон строк ПО СРОКУ** (заявка: «дата
   которых прошла — светлее, ещё не наступила или не прошла — темнее»):
   - `.ws-ep-past` (дата_окончания строго < «сегодня», ставит
     `_renderMonthEventsPanel` через `this._isoDate(new Date())`;
     однодневные без конца — по дате_начала): тёмная
     rgba(255,255,255,0.12) ≈ rgb(43,50,60) / светлая
     rgba(255,255,255,0.55) ≈ rgb(245,244,240) — СВЕТЛЕЕ окна.
   - База `.ws-ep-item` (идёт сегодня/будущие): тёмная rgba(0,0,0,0.45)
     ≈ rgb(7,12,18) / светлая rgba(0,0,0,0.12) ≈ rgb(204,203,195) —
     ТЕМНЕЕ окна. Контраст past↔upcoming Δ36-44 ед. RGB.
   - Плашка padding 2px 6px + margin 0 -6px; порядок каскада
     0,1,0 → 0,2,0 → 0,2,0 → 0,3,0 (светлые правила после базовых).
2. **Мобайл ≤1023px — запрет выделения текста** (заявка: «не должен
   выделяться текст в ячейках для копирования, поделиться, поиска в
   гугл»): `user-select:none` (+префиксы, `-webkit-touch-callout:none`)
   на `.ws-grid` и `.ws-tt-table` — все ячейки шахматки (значения/ФИО/
   шапка) и итогов (месяц/год/архив). Десктоп НЕ тронут.

## Версии / проверки

- SW: kipia-v456 → **kipia-v457** (guard v458), 69 тест-файлов бампнуто.
- Тесты: **3252 passed / 0 failed** (было 3232; +20 test-task380.js с
  маппингом kipia-test-v609→kipia-v457 / v610→v458; адаптации 315/316 —
  заглушка _isoDate в 4 VM-хостах, тот же адапт-скрипт).
- Браузер-проверка: **30/30** (порт 8989; localStorage БЕЗ префикса —
  kip8_session_token/app-theme, урок 365; десктоп dark+light: классы
  past = 2 по правилу «окончание < сегодня 2026-09-17», computed фоны
  точные, blend-яркость past > окна > upcoming, плашка, десктоп-
  выделение живо; мобайл 375: user-select:none сетка/ФИО/значения/
  шапка/итоги, программная Selection ПУСТА; 0 JS-ошибок).
- Пиксельные пруфы: тёмная — rgb(43,50,60)×2 + rgb(7,12,18)×3 на окне
  rgb(14,22,33); светлая — rgb(245,244,240)×2 + rgb(204,203,195)×3 на
  rgb(233,231,222) — раскладка 2+3 совпадает с расчётом.
- VLM: бинарное сравнение чистых фоновых полос — «верхняя (прошедшая)
  светлее» в ОБОИХ темах ✓.

## Серверных действий НЕТ

Чистый клиентский патч. .gs-файлы НЕ менялись. Десктоп-зеркала
синхронизирует CI (sync-to-desktop).

## Развёртывание

1. Этот коммит уже в main kip8 (GitHub Pages задеплоится CI).
2. Пользователям: обновить страницу 2 раза (смена SW v456→v457).
