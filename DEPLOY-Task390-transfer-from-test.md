# DEPLOY — Task 390 (боевой kip8): перенос из kip8test — «Общая» вкладка «Работников»

**Дата:** 2026-09-22 · **Коммит:** `2df8aa0` · **SW:** `kipia-v465` → `kipia-v466` (guard `v467`) · **Источник:** kip8test `99464ba`

## Состав переноса (заявка Task 390)

1. Сноска «Сводка по всем работникам: сменных — N, дневных — N. Полная карточка каждого работника — на его вкладке.» — УДАЛЕНА.
2. Шапка «Общей» вкладки — две строки:
   - `Работников по штату 14: 2 мастера; 7 дневных; 5 сменных.` (константа, первой строчкой);
   - ниже `Работников на текущий момент: N мастера; N дневных; N сменных.` — АВТОМАТИЧЕСКИЙ ПОДСЧЁТ ПО КАТЕГОРИЯМ (мастера — должность «Мастер КИПиА» через `_isMasterKipia`; дневные/сменные — по типу БЕЗ мастеров; склонения).
3. Шапка ВЫДЕЛЕНА фоном (`#1E2B42` тёмная / `#E4E0D3` светлая + рамка/радиус/паддинги), кнопка «Добавить работника» внутри шапки.
4. Ярлыки ПРИМЫКАЮТ к окну вкладок (правый отступ колонки убран).
5. Тёмная тема: ярлыки — ТЁПЛЫЙ светлый тон `#4B4E46`/`#575A50`/`#63665B` (не сливаются с общим фоном страницы `#1a2233`); светлая — без изменений.

## Механика переноса

- `scripts/task390-patch.py` (из kip8test, применён в корне kip8) — 11 правок `index.html`, все якоря совпали; зоны Task 390 побайтово идентичны kip8test.
- `scripts/task390-transfer.py` — бамп тестов (guard `kipia-v466`→`v467`, ассерты `kipia-v465`→`v466`; 78 файлов) + `sw.js` `kipia-v465`→`kipia-v466` + копия `tests/test-task390.js` (маппинг `kipia-test-v618`→`kipia-v466`, `v619`→`v467`) + `run-all.js`.
- `../kip8test/scripts/task390-adapt-tests.py` (в корне kip8) — 17 правок живых тестов (task385 ×2, task388 ×6, task389 ×9).

## Серверные шаги

**НЕТ** — чистый клиент.

⚠️ Открытые серверные шаги ПРЕЖНИЕ (за пользователем, один бэкенд Apps Script на оба сайта):
- **Task 384 (ОБЯЗАТЕЛЕН для «Сохранить» правок карточек):** `Code.gs` (2 case) + `WorkSchedule.gs` (updateEmployee/updateVacation) + «Новая версия» — см. `DEPLOY-Task384-transfer-from-test.md`.
- Task 387 (опционален — нормализация кодов на клиенте).

## Проверки

- Тесты: **3540/0** (`node tests/run-all.js`; 3518 + 22 task390).
- Браузер: **24/24** (`scripts/task390-browser-check.py`, порт 9003, localStorage без префикса — результаты идентичны kip8test).
- VLM: тёмная тема kip8 — шапка выделена, ярлыки тёплые контрастные, примыкание, сноски нет.
- CI ×4 success (Tests / Sync to kip8-desktop / Build Desktop App / pages build).
- **Прод:** `CACHE_VERSION = 'kipia-v466'`; все маркеры Task 390 в живом `index.html`; старых формулировок («На текущий момент:», «По штату: 14, из которых», `ws-wgen-note`) — 0.
