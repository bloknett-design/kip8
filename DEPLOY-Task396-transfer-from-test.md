# Task 396 — перенос из kip8test: зебра строк + кнопки в шапках блоков + оглавления

**Дата:** 23.09.2026
**Репо:** kip8 (боевой сайт)
**SW:** kipia-v471 → **kipia-v472**
**Коммит-источник:** kip8test 6826330 (Task 396)

## Что перенесено (заявка, 3 части)

1. **Зебра строк в блоках карточек** — чередующийся фон вторых
   строк каждого блока (профиль/отпуска/мероприятия/СИЗ), класс
   `ws-row-alt`, оттенок зебры сводной таблицы «Общей» вкладки
   (тёмная `rgba(255,255,255,0.045)`, светлая `rgba(0,0,0,0.05)`);
   строки — «пилюли» (паддинг 10px, скругление 6px); только у
   блоков-окон страницы «Работники» — попап шахматки без зебры;
   видна и зрителям (отображение, не правка).
2. **Компактные кнопки в верхнем правом углу блоков** —
   «Правка данных…» (акцент) и «Уволить…» (красная) в шапке
   блока профиля; «+ Отпуск…»/«+ Мероприятие…»/«+ СИЗ…» — в
   шапках своих блоков; 26px/12px, стиль сайта (синий/оранжевый
   по теме); контракты onclick и классы-маркеры прежние;
   легаси-строки прежнего вида — гейт `withEdit && !asBlocks`.
3. **Оглавления блоков** — полоса `.ws-whead` на всю ширину окна
   с ДРУГИМ фоном (#1E2B42 тёмная / #E4E0D3 светлая — как шапка
   сводки Task 390); ФИО 16px/700 без капса, секции 13px/700
   капс, первичный цвет (было 15px/600 и 12px/600 вторичный).

## Перенос

- `scripts/task396-patch.py` — копия kip8test, **19/19 якорей**
  совпали с первого прохода (зоны Task 396 побайтово идентичны).
- `scripts/task396-adapt-tests.py` — копия, 4 ассерта
  test-task393 + 1 ассерт test-task318.
- `scripts/task396-transfer.py` — копия `tests/test-task396.js`
  с маппингом SW-версий (kipia-test-v623→v471, v624→v472,
  v625→v473 — пост-бамп форма); регистрация в `run-all.js`;
  копия браузерной проверки (порт 9007, localStorage БЕЗ префикса
  `kip8test:`, тег t396k8, пруфы task396k8-*). Подводный камень
  (исправлен в скрипте): замены localStorage — НА УРОВНЕ ИСХОДНИКА
  (литералы), НЕ склеенного значения; контроль — строгий
  (в блоке attach не осталось `localStorage.setItem('kip8test:…`).
- `scripts/task396-bump-sw.py` — SW kipia-v471→v472 (guards
  v472→v473 первыми, 85 файлов, свой тест исключён — пост-форма).

## Проверки

- Тесты: **3691/0** (3672 + 19 перенесённых).
- Браузер (порт 9007, Playwright): **32/32** — результаты
  идентичны kip8test (тёмная: 4 шапки #1E2B42 во всю ширину,
  5 кнопок 26px в углах шапок, зебра 2/4-х строк всех блоков,
  легаси-строк нет, попап прежний; view: шапки+зебра без кнопок;
  мобайл 375 светлая: шапка #E4E0D3, кнопки #C6613F/#c62828,
  зебра 0.05, перенос кнопок; 0 JS-ошибок ×3). VLM ×1
  (glm-5v-turbo — подтверждено).
- Пруфы: `task396k8-proof-*.png`.

## Серверные шаги

**НЕТ** — Apps Script не тронут (чистый клиент).

## ⚠️ Статус деплоя

Локальный коммит b1af7fa готов; push в GitHub заблокирован
утратой PAT после отката песочницы (см. worklog kip8test:
восстановление Task 395 из origin). После переотправки PAT
пользователем: `git push` → CI ×4 (Tests/Sync-to-desktop/Build
Desktop App/pages build) → прод проверит SW kipia-v472 и маркеры
Task 396 (`ws-whead`, `ws-wbtn`, `ws-row-alt`, «в углу»).
